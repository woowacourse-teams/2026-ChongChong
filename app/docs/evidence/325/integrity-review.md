# Visual QA Pass A 재검토

VERDICT: PASS
CONFIDENCE: HIGH

초기 P1 대상 생성 결함은 수정됐다. AssignmentProvider.tsx는 타인을 `selectedStudy.members - 1`명 생성하고 `leaderParticipates`가 참이면 self를 별도로 추가한다. 1인 스터디에서 실제 작성 및 리더 제출 완료까지 검증했다.

## 실제 Chrome 회귀

- 별도 탭 1563764327, localhost:8083 사용. root/Figma 탭은 조작하지 않았다.
- 스터디 `QA 1인 대상 회귀` 생성 → 스터디 목록 `1명` 확인.
- 해당 스터디에 `1인 리더 제출 검증` 과제 작성, 마감 2026-08-06 18:00(앱 고정 previewClock 기준 미래), 참가 기본값 유지.
- 생성 후 요약 `0 / 1명`, `미제출 1명`, `바니 (나)` 확인.
- 상세 `과제 제출하기` 진입 → 내용 `1인 리더 제출 성공 확인` 입력 → 제출.
- `/assignments/assignment-1789453740194?tab=detail` 복귀, `방금 제출`, 작성한 본문, `제출물 수정하기` 확인.

## 소스 확인

비공개 타인 제출의 요약/직접 경로 차단, UI와 Provider의 역할 검사, 대상·마감·링크 검증, byStudy 데이터 분리, 시나리오/로그아웃 resetActivity 연계, 홈/스터디 목록 Provider 파생은 유지됐다. 공통 토큰과 프리미티브를 사용하며 실제 컴포넌트 트리다.

## 한계

이번 런타임 회귀는 발견한 1인 리더 참가 결함에 집중했다. 기본 5인 및 리더 제외는 수정 소스로 확인했으며 이번 재검토에서 별도 UI 실행하지 않았다. 파일 선택·다양한 뷰포트·네이티브 실행·이미지 diff 정량 판정은 이 pass에서 수행하지 않았다. 이전 목록 캡처 직접 확인 결과는 초기 보고서 참조.

BLOCKING: 없음.
